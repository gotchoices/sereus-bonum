diff --git a/dist/src/cluster/cluster-repo.js b/dist/src/cluster/cluster-repo.js
index 188bf70b9a0d07f6c079899bac4572b8a91572b3..8a85dda69994b55384c03c09d36107faa26b8a09 100644
--- a/dist/src/cluster/cluster-repo.js
+++ b/dist/src/cluster/cluster-repo.js
@@ -119,12 +119,12 @@ export class ClusterMember {
         this.membershipAdmissionFraction = consensusConfig?.membershipAdmissionFraction ?? 0.75;
         this.configuredClusterSize = consensusConfig?.clusterSize;
         this.allowUnvalidatedSmallCluster = consensusConfig?.allowUnvalidatedSmallCluster ?? false;
-        // Periodically clean up expired transactions (.unref() so tests/short-lived processes can exit)
+        // Periodically clean up expired transactions (?.unref?.() so tests/short-lived processes can exit)
         this.expirationInterval = setInterval(() => this.queueExpiredTransactions(), 60000);
-        this.expirationInterval.unref();
+        this.expirationInterval?.unref?.();
         // Process cleanup queue
         this.cleanupInterval = setInterval(() => this.processCleanupQueue(), 1000);
-        this.cleanupInterval.unref();
+        this.cleanupInterval?.unref?.();
     }
     /**
      * The resolved super-majority threshold this member runs on. Exposed so the composition root can
@@ -182,7 +182,7 @@ export class ClusterMember {
             // Remove from pending updates after a short delay to allow concurrent calls to see it
             setTimeout(() => {
                 this.pendingUpdates.delete(record.messageHash);
-            }, 100).unref();
+            }, 100)?.unref?.();
         }
     }
     async processUpdate(record) {
@@ -1161,7 +1161,7 @@ export class ClusterMember {
         let timer;
         const timeout = new Promise((_, reject) => {
             timer = setTimeout(() => reject(new Error(`reconcile for block ${blockId} timed out after ${ReconcileTimeoutMs}ms`)), ReconcileTimeoutMs);
-            timer.unref();
+            timer?.unref?.();
         });
         return Promise.race([promise, timeout]).finally(() => {
             if (timer)
@@ -1176,8 +1176,8 @@ export class ClusterMember {
             return {};
         }
         return {
-            promiseTimeout: setTimeout(() => this.handleExpiration(record.messageHash), record.message.expiration - Date.now()).unref(),
-            resolutionTimeout: setTimeout(() => this.resolveWithPeers(record.messageHash), record.message.expiration + 5000 - Date.now()).unref()
+            promiseTimeout: setTimeout(() => this.handleExpiration(record.messageHash), record.message.expiration - Date.now())?.unref?.(),
+            resolutionTimeout: setTimeout(() => this.resolveWithPeers(record.messageHash), record.message.expiration + 5000 - Date.now())?.unref?.()
         };
     }
     hasConflict(record) {
diff --git a/dist/src/libp2p-node-base.js b/dist/src/libp2p-node-base.js
index a2a9025a8c062351c081982ecd2ab87aca953916..254f76fb1bd267d293c9a85fd4e9f1ef676ed5e8 100644
--- a/dist/src/libp2p-node-base.js
+++ b/dist/src/libp2p-node-base.js
@@ -398,7 +398,7 @@ export async function createLibp2pNodeBase(options, defaults) {
         try {
             const response = await Promise.race([
                 syncClient.requestBlock({ blockId, rev: undefined }),
-                new Promise(resolve => { setTimeout(() => resolve({ success: false }), 1000).unref(); })
+                new Promise(resolve => { setTimeout(() => resolve({ success: false }), 1000)?.unref?.(); })
             ]);
             return response.success ? response.archive : undefined;
         }
